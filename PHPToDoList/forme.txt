** For me 
Need to sanitize inputs!!
Step 1. USE PDO || mysqli

Step 2. VALIDATE inputs 
         - VAlidate Date through DateTime  class
         - Date should be more than Current Date and less than 31 years from Current Date

         date format is like 2012-08-06 (yyyy-mm-dd)

         -Validate Strings
         -Escape html special chars and remove HTML

Step 3. SANITIZE inputs
        //FILTER_SANITIZE_STRING
        trim string too!

        sanitize date

EXAMPLE VALIDATION AND SANITIZATION
/*
$str = "<h1>Hello WorldÆØÅ!</h1>";

// Remove HTML tags and all characters with ASCII value > 127
$newstr = filter_var($str, FILTER_SANITIZE_STRING, FILTER_FLAG_STRIP_HIGH);
echo $newstr;
*/

